# Numerical appendix

## Canonical candidates

See `outputs/final_predictions.csv` for machine-readable values.

## Threshold certificate

See `outputs/threshold_certificate.json` for the exhaustive certificate.

## Source inventory

See `archive_index/source_inventory.csv` and `.json`.
